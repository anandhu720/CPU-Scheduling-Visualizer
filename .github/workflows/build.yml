name: Build

on:
  pull_request:
    branches: ["main"]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo '${{ toJSON(github.event) }}' | jq
        shell: bash
